#include <stdio.h> 

char a[101]; //자연수개수

int digit_sum(int x) //자연수의 자릿수 합을 구하는 함수 
{
	int temp, sum = 0;
	while(x > )
	
}

int main() {
	freopen("input.txt", "rt", stdin);
	int n, num, sum, max = -21470000000, result;
	scanf("%d", n);
	for(int i = 0; i < n; ++i)
	{
		scanf("%d", &num);
		sum = digit_sum(num);
	}
	return 0;
}
